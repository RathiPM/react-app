import React from "react"; //JSx is converted to react

class Login extends React.Component {
    render() { //render-kind of object
        return (
            <div>
                <h1>Class Component Example</h1>
            </div> //we can empty angle bracket also,but not preferred.
        );

    }
}

export default Login;